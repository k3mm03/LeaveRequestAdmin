<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.newwavetech.leaverequestadmindemo1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.AdminLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AdminManagementActivity" />
        <activity android:name=".activities.GridItemEmployeeActivity"/>
        <activity android:name=".activities.GridItemDepartmentActivity"/>
        <activity android:name=".activities.EmployeeDetailActivity"/>
        <activity android:name=".activities.GridItemHistoryActivity"/>
        <activity android:name=".activities.RequestDetailActivity"/>
        <activity android:name=".activities.DecisionDetailActivity"/>
    </application>

</manifest>